#plots ascii csv data downloaded from bepicolombo quicklook
# http://bepicolombo.esac.esa.int/bscs-qla/home
# needs GNU toolchain: bash, awk, sed on linux or MS windows with msys2/mingw or apple macOS
#usage: ./bcplot.sh filename.csv 
#e. g.
./bcplot.sh berm1.csv

#to also create a data file with timestamps column converted to
#seconds numerical values (field separator = space):
./creategpplot.sh infile.csv outfile.ssv

